# Live2D Master Agent v0.10.0 - Python Requirements
# Installation: python -m pip install -r requirements.txt

# ====== Core Dependencies (Required) ======
Pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
urllib3>=2.0.0
httpx>=0.24.0
aiohttp>=3.9.0
aiofiles>=23.0
websockets>=12.0
psd-tools>=1.9.0
scipy>=1.10.0
scikit-learn>=1.3.0
cryptography>=41.0.0

# ====== Optional: API server (api_server.py, 与 Go API 并行的 Python 实现) ======
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# ====== CLI Enhancement ======
rich>=13.0.0

# ====== Desktop Pet ======
pygame>=2.5.0; python_version < "3.14"
pygame-ce>=2.5.0; python_version >= "3.14"

# ====== Image Processing ======
opencv-python>=4.8.0
onnxruntime>=1.14.0
rembg[cpu]>=2.0.0

# ====== Face Tracking (drivers/face_tracker) ======
mediapipe>=0.10.0

# ====== Audio Capture (drivers/audio) ======
sounddevice>=0.4.6
soundfile>=0.12.0

# ====== TTS (llm_bridge/tts) - free Edge TTS ======
edge-tts>=6.1.0

# ====== Async / Web (for Go API bridge + web frontend) ======
# aiohttp>=3.9.0        # already required above
# aiofiles>=23.0        # already required above
# httpx>=0.24.0         # already required above
# websockets>=12.0      # already required above

# ====== Optional: CLIP embedding (for character visual embeddings) ======
# Install with: pip install torch transformers
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0

# ====== Live2D moc3 compile & verification (P0) ======
# py-moc3: 结构参考与交叉校验（读写 .moc3）；编译器本体自研，不依赖其运行时
py-moc3>=0.1.0
# live2d-py: 官方 Cubism Native Core，仅用于隔离验证（逐像素验收 / 原生渲染探针），
# 不进入生产数据路径。它没有预编译 wheel，构建需要 cmake + C++ 工具链，
# 因此不能作为默认依赖（否则 `pip install -r requirements.txt` 及 Docker 构建会失败）。
# 需要官方内核验收时再单独安装（CI 的 moc3-acceptance job 即如此）：
#   pip install live2d-py
# live2d-py>=0.7.0

# ====== Optional: AI Models (large, install separately with install_models.py) ======
# segment-anything>=1.0
# diffusers>=0.20.0
# accelerate>=0.20.0
# open-clip-torch>=2.20.0
# faster-whisper>=0.10.0
# funasr>=1.0.0
# huggingface-hub>=0.17.0
